import Hero from "@/components/Hero";
import React from "react";
import Header from "../components/Header";

const page = () => {
   return (
      <div>
         <Header />
         <Hero />
      </div>
   );
};

export default page;
